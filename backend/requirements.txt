fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic[email]==2.10.4
pydantic-settings==2.7.1
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
qdrant-client==1.12.1
openai==1.59.6
python-dotenv==1.0.1
python-multipart==0.0.20

# Auth (Phase 2): password hashing, JWT, Google OAuth, forgot-password email
# bcrypt is pinned <4.1: passlib 1.7.4's internal wrap-bug self-test
# crashes with a hard ValueError on bcrypt>=4.1 (bcrypt 5.x confirmed
# broken here) instead of the ValueError passlib expects to catch.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
authlib==1.4.0
httpx==0.28.1
aiosmtplib==3.0.2
itsdangerous==2.2.0
