--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic[email]==2.10.4
pydantic-settings==2.7.1
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
qdrant-client==1.12.1
openai==1.59.6
python-dotenv==1.2.2
python-multipart==0.0.31

# Auth (Phase 2): password hashing, JWT, forgot-password email
# bcrypt is pinned <4.1: passlib 1.7.4's internal wrap-bug self-test
# crashes with a hard ValueError on bcrypt>=4.1 (bcrypt 5.x confirmed
# broken here) instead of the ValueError passlib expects to catch.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.4.0
httpx==0.28.1
aiosmtplib==5.1.2

# Document ingestion / RAG engine (this phase): PDF/DOCX text extraction
pdfplumber==0.11.4
python-docx==1.1.2

# OCR (images + scanned PDF pages): EasyOCR pulls in torch/torchvision -
# the --extra-index-url line at the top of this file makes those resolve
# to CPU-only wheels instead of the much larger CUDA builds. pymupdf
# (import name `fitz`) renders a scanned PDF page to an image for OCR with
# no system binary needed (bundled), unlike poppler/ghostscript approaches.
easyocr==1.7.2
pymupdf==1.25.1

# Testing (backend/tests/): FastAPI's TestClient needs httpx (already
# present above); pytest-asyncio is included for any future async test
# functions even though the current suite is entirely synchronous
# (TestClient wraps async endpoints transparently).
pytest==9.0.3
pytest-asyncio==1.4.0
